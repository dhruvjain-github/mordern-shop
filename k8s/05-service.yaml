# Service to expose the shopping app
apiVersion: v1
kind: Service
metadata:
  name: shopping-service
  namespace: shopping-app
spec:
  selector:
    app: shopping-app
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP
